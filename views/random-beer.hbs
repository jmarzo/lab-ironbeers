<div class='container'>
  <h1>Random Beer</h1>
  {{#each randomBeer}}
    <h2>{{name}}</h2>
    <h6>{{tagline}}</h6>
    <img src={{image_url}} alt='Beer ' {{name}} />
    <p>{{description}}</p>
  {{/each}}
</div>